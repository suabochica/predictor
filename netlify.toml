# This file is no longer used directly.
# Each app has its own netlify.toml:
#   apps/gateway/netlify.toml   — main site
#   apps/polla/netlify.toml     — /polla/
#   apps/fantasy/netlify.toml    — /fantasy/
#
# Deploy each as a separate Netlify site with:
#   Base directory: apps/<app>
#   Build command:  pnpm build
#   Publish dir:    dist
