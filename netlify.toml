# This is a monorepo with multiple Netlify sites.
# Each app has its own netlify.toml with base directory set in the dashboard:
#
#   Gateway (main)     → base: apps/gateway → apps/gateway/netlify.toml
#   Polla (proxy)      → base: apps/polla   → apps/polla/netlify.toml
